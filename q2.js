//Question(4.2)
//Write a program that prints the cube of the number input by the user.
function cubeCalculate() {
	let userInput = document.querySelector("input").value;
	let cube = userInput * userInput * userInput;
	alert(cube);
}
//Answer(2 ka output 8)
